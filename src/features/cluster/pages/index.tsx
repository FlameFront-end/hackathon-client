import { type FC } from 'react'
import { ClusterWrapper } from './Cluster.styled.tsx'

const Cluster: FC = () => {
    return (
        <ClusterWrapper>
            <div>Cluster</div>
        </ClusterWrapper>
    )
}

export default Cluster
